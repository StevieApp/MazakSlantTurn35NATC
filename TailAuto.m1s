If (GetOEMLED(809) Or GetOEMLED(807)) Then	Message "Please Home Machine First"	EndElseEnd IfZPositionLastCouple = 1821LastCouple = GetUserDRO(ZPositionLastCouple)Zaxis = 2CurrPosZ = GetABSPosition(Zaxis)Message LastCouple = -2000Call TailAutoSeek()Sub TailAutoSeek	If LastCouple = -2000 Then		Message "Tail Coupled/Issue Autocoupling"		Sleep(1000)		Message "Do manual coupling"	Else		If(CurrPosZ>LastCouple) Then			If RunScript("seektailleft") < 0 Then  				Message "RunScript returned an error"			Else 			End If		Else			If RunScript("seektailright") < 0 Then  				Message "RunScript returned an error"			Else 			End If		End If	End IfEnd Sub